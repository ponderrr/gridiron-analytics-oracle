import React from "react";
import Layout from "@/components/Layout";
import PlaceholderMessage from "@/components/ui/PlaceholderMessage";

const Profile: React.FC = () => {
  return (
    <Layout>
      <PlaceholderMessage message="Profile coming soon…" />
    </Layout>
  );
};

export default Profile;
